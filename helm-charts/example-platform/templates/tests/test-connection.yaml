apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "example-platform.fullname" . }}-test-connection"
  labels:
    {{- include "example-platform.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "example-platform.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
